Each iteration is tested using the following:

(A) randomness_tests.py

(B) rng_full_analysis.ipynb

The results for (A) are found in the .md file inside each folder and the same for (B) is found inside the file itself.
